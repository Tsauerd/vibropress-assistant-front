# VibroPress AI — Landing Page

Статическая первая страница для VibroPress AI, готовая к деплою на Netlify.

## 📁 Структура файлов

```
vibropress-landing/
├── index.html       # Основная HTML страница
├── styles.css       # Все стили
├── script.js        # JavaScript для интерактивности
└── README.md        # Этот файл
```

## 🚀 Деплой на Netlify

### Способ 1: Drag & Drop (самый простой)

1. Зайди на [netlify.com](https://netlify.com)
2. Зарегистрируйся или войди
3. Перетащи всю папку с файлами в окно Netlify
4. Готово! Сайт будет доступен по адресу `your-site-name.netlify.app`

### Способ 2: Через GitHub

1. Создай репозиторий на GitHub
2. Загрузи эти файлы в репозиторий
3. На Netlify нажми "New site from Git"
4. Выбери свой репозиторий
5. Настройки билда оставь пустыми (статический сайт)
6. Нажми "Deploy"

### Способ 3: Netlify CLI

```bash
# Установи Netlify CLI
npm install -g netlify-cli

# Войди в аккаунт
netlify login

# Задеплой сайт
netlify deploy --prod
```

## 🎨 Что включено

### Функционал
- ✅ Адаптивный дизайн (мобилки + планшеты + десктоп)
- ✅ Плавная прокрутка по якорям
- ✅ Подсветка активного пункта меню при скролле
- ✅ Мобильное меню (бургер)
- ✅ Анимации появления элементов
- ✅ Hover эффекты на карточках
- ✅ **Интерактивное демо-окно чата** с 4 режимами работы
- ✅ **Примеры вопросов** для каждого режима (кнопки быстрого ввода)
- ✅ **Имитация работы бота** (typing indicator, задержки ответов)
- ✅ **Переключение режимов** с обновлением примеров вопросов

### Секции
1. **Hero** — название + описание + CTA кнопки
2. **Интерактивное демо** — окно чата с переключателями режимов:
   - 📋 ГОСТ/СП — поиск по нормативке
   - ⚙️ Оборудование — мануалы и настройки
   - 🔍 Претензии — анализ дефектов
   - 🧪 Рецептуры — составы и режимы
3. **Возможности** — 4 карточки с функциями бота
4. **Как работает** — 3 шага процесса
5. **Для кого** — 4 бейджа целевых аудиторий
6. **CTA** — финальный призыв к действию
7. **Footer** — контакты и обратная связь

## ⚙️ Настройка

### Изменить ссылку на бота

В файле `script.js` найди:

```javascript
if (targetId === '#bot') {
    alert('Бот скоро будет доступен!');
    return;
}
```

Замени на реальную ссылку:

```javascript
if (targetId === '#bot') {
    window.location.href = 'https://your-bot-url.com';
    return;
}
```

Или в `index.html` замени все `href="#bot"` на `href="https://your-bot-url.com"`.

### Изменить контакты в футере

В файле `index.html` найди секцию `<footer>` и замени:

```html
<a href="https://t.me/yourusername" class="footer-link">Telegram</a>
<a href="mailto:contact@example.com" class="footer-link">Email</a>
```

### Изменить цвета

В файле `styles.css` в начале найди переменные:

```css
:root {
    --primary-color: #2563eb;      /* Основной синий */
    --primary-dark: #1e40af;       /* Тёмный синий */
    /* ... */
}
```

Можешь заменить на любые другие цвета, например:

```css
--primary-color: #10b981;  /* Зелёный */
--primary-dark: #059669;   /* Тёмно-зелёный */
```

## 📱 Тестирование

Открой `index.html` локально в браузере:
- Проверь на разных размерах экрана (F12 → Device Toolbar)
- Протестируй все якорные ссылки
- Проверь мобильное меню
- Убедись, что все анимации работают

## 🎯 Что дальше?

После деплоя можешь:

1. **Подключить свой домен** в настройках Netlify
2. **Добавить Google Analytics** (вставь код перед `</head>`)
3. **Добавить блок "Примеры вопросов"** для повышения конверсии
4. **Сделать форму обратной связи** (через Netlify Forms)
5. **Добавить демо-видео** в Hero секцию

## 📊 Рекомендации по оптимизации

- ✅ Все стили inline (нет внешних зависимостей)
- ✅ Системные шрифты (быстрая загрузка)
- ✅ Минимальный JS (только необходимое)
- ✅ Семантическая разметка (SEO)
- ✅ Meta теги для соцсетей (можно добавить Open Graph)

Удачи с запуском! 🚀
